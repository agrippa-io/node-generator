import { expect } from 'chai'
import {{ pascalCase associationName }}ReadController from '../../../../controllers/{{ pascalCase model }}/associations/{{ pascalCase model }}.{{ pascalCase associationName }}.Read'

describe.skip('Association Controller - {{ pascalCase model }} - {{ pascalCase associationName }} - Read', () => {
  beforeEach(() => {
    // Setup
  })

  it('should do something', () => {
    // Test it
    expect(true).to.equal(false)
  })
})
