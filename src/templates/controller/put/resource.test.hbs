import { expect } from 'chai'

describe.skip('{{ pascalCase model }} Controller - updateResource', () => {
  beforeEach(() => {
    // Setup
  })

  it('should do something', () => {
    // Test it
    expect(true).to.equal(false)
  })
})
