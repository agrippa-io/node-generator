import { expect } from 'chai'
import {{ pascalCase model }}AssociationRouter from '../../../routes/{{ pascalCase model }}/association.router'

describe.skip('{{ pascalCase model }}', () => {
  beforeEach(() => {
    // Setup
  })

  it('should do something', () => {
    // Test it
    expect(true).to.equal(false)
  })
})
