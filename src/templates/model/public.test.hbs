import { expect } from 'chai'
import {{ pascalCase model }}Public from '../../../models/{{ pascalCase model }}/public'

describe.skip('{{ pascalCase model }}Public', () => {
  beforeEach(() => {
    // Setup
  })

  it('should do something', () => {
    // Test it
    expect(true).to.equal(false)
  })
})
