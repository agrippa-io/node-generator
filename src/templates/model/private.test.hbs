import { expect } from 'chai'
import {{ pascalCase model }}Private from '../../../models/{{ pascalCase model }}/private'

describe.skip('{{ pascalCase model }}Private', () => {
  beforeEach(() => {
    // Setup
  })

  it('should do something', () => {
    // Test it
    expect(true).to.equal(false)
  })
})
