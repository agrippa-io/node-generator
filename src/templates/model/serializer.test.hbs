import { expect } from 'chai'
import {{ pascalCase model }}Serializer from '../../../models/{{ pascalCase model }}/serializer'

describe.skip('{{ pascalCase model }}Serializer', () => {
  beforeEach(() => {
    // Setup
  })

  it('should do something', () => {
    // Test it
    expect(true).to.equal(false)
  })
})
