import { expect } from 'chai'
import {{ camelCase associationName }} from '../../../../models/{{ pascalCase model }}/virtuals/get.{{ camelCase associationName }}'

describe.skip('{{ pascalCase model }} - virtual - get.{{ camelCase associationName }}', () => {
  beforeEach(() => {
    // Setup
  })

  it('should do something', () => {
    // Test it
    expect(true).to.equal(false)
  })
})
